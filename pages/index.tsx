import type { NextPage } from "next";
import Cursor from "../components/layout/cursor";

const Home: NextPage = () => {
  return (
    <div className={"h-64"}>
      <Cursor/>
      hello
    </div>
  );
};

export default Home;
